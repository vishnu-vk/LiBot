token: "qDoy0VWAB7GxRZor"    # lichess OAuth2 Token
url: "https://lichess.org/"  # lichess base URL

engine:                      # engine settings
  dir: "./engines/"          # dir containing engines, relative to this project
  name: "stockfish"        # binary name of the engine to use
  protocol: "uci"            # "uci" or "xboard"
  polyglot:
    enabled: false           # activate polyglot book
    book: 
      - "engines/books/Book.bin"
      - "engines/books/codekiddy.bin"
      - "engines/books/DCbook_large.bin"
      - "engines/books/Elo2400.bin"
      - "engines/books/final-book.bin"
      - "engines/books/gavibook.bin"
      - "engines/books/gavibook-small.bin"
      - "engines/books/gm2600.bin"
      - "engines/books/komodo.bin"
      - "engines/books/KomodoVariety.bin"
      - "engines/books/Performance.bin"
      - "engines/books/varied.bin"
    max_depth: 30             # half move max depth
    min_weight: 1            # selects move with highest weight but not below this value
    selection: "best_move"
  uci_options:               # arbitrary UCI options passed to the engine
    Move Overhead: 700       # increase if your bot flags games too often
    Threads: 4               # max CPU threads the engine can use
#    Hash: 256                # max memory (in megabytes) the engine can allocate
#   go_commands:             # additional options to pass to the UCI go command
#      nodes: 1               # Search so many nodes only.
#       depth: 15               # Search depth ply only.
#     movetime: 1000         # Integer. Search exactly movetime milliseconds.
# xboard_options:            # arbitrary xboard options passed to the engine
#   cores: "4"
#   memory: "4096"
#   egtpath:                 # dir containing egtb, relative to this project
#     gaviota: "Gaviota path"
#     nalimov: "Nalimov Path"
#     scorpio: "Scorpio Path"
#     syzygy: "Syzygy Path"
  silence_stderr: false      # some engines (yes you, leela) are very noisy

abort_time: 20               # time to abort a game in seconds when there is no activity
fake_think_time: false      # artificially slow down the bot to pretend like it's thinking

challenge:                   # incoming challenges
  concurrency: 1             # number of games to play simultaneously
  sort_by: "best"            # possible values: "best", "first"
  accept_bot: true          # accepts challenges coming from other bots
  max_increment: 180         # maximum amount of increment to accaept a challenge. the max is 180. set to 0 for no increment
  min_increment: 0
  variants:                  # chess variants to accept (http://lichess.org/variant)
    - standard
    - fromPosition
    - antichess
    - atomic
    - chess960
    - crazyhouse
    - horde
    - kingOfTheHill
    - racingKings
    - threeCheck
  time_controls:             # time controls to accept
    - ultraBullet
    - bullet
    - blitz
    - rapid
    - classical
    - correspondence
  modes:                     # game modes to accept
#    - casual                 # unrated games
     - rated                  # rated games - must comment if the engine doesn't try to win